using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Shield : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {

        activeShield();
    }

    private void activeShield()
    {
        //enemyStat enemy = transform.parent.GetComponentInParent<enemyStat>();
        //if (enemy != null && enemy != GetComponent<enemyStat>())
        //{
        //    enemy.isShield = true;
        //}
    }
}
